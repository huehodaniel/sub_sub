<%= form_with(model: subscription, url: url, method: method, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone_model %>
    <%= form.text_field :phone_model %>
  </div>

  <div class="field">
    <%= form.label :imei %>
    <%= form.text_field :imei %>
  </div>

  <div class="field">
    <%= form.label :full_price %>
    <%= form.text_field :full_price %>
  </div>

  <div class="field">
    <%= form.label :payments %>
    <%= form.number_field :payments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
